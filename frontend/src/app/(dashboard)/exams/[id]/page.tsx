// Screen: Edit Exam — view exam key + papers table (Check Paper / Delete)
export default function EditExamPage({ params }: { params: { id: string } }) {
  return <div>Edit Exam: {params.id}</div>;
}
